---
apiVersion: bootstrap.cluster.x-k8s.io/v1beta1
kind: KubeadmConfigTemplate
metadata:
  name: ${CLUSTER_NAME}-md-0
spec:
  template:
    spec:
      preKubeadmCommands:
        - curl -fsSL https://raw.githubusercontent.com/linode/cluster-api-provider-linode/fc6c484b882d5e7a5f2f9c4015542e73d17091e2/scripts/pre-kubeadminit.sh | bash -s ${KUBERNETES_VERSION} ${PROXY_CRI_TOOLS_URL:=""} ${PROXY_K8S_RELEASE_URL:=""}
        - hostnamectl set-hostname '{{ ds.meta_data.label }}' && hostname -F /etc/hostname
      joinConfiguration:
        nodeRegistration:
          kubeletExtraArgs:
            cloud-provider: external
          name: '{{ ds.meta_data.label }}'
