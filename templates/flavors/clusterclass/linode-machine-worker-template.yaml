apiVersion: infrastructure.cluster.x-k8s.io/v1alpha1
kind: LinodeMachineTemplate
metadata:
  name: ${CLUSTER_CLASS_NAME}-worker
spec:
  template:
    spec:
      # image is a required field (OpenAPI schema).
      image: REPLACEME
      # type is a required field (OpenAPI schema).
      type: REPLACEME
      # region is a required field (OpenAPI schema).
      region: REPLACEME
